import { HostDirectivesDirective } from './host-directives.directive';

describe('HostDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new HostDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
